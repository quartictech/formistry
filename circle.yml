machine:
  python:
    version: 3.6.0
  services:
    - docker

test:
  override:
    - echo "Nowt"

deployment:
  hub:
    branch: /.*/
    commands:
      - curl https://raw.githubusercontent.com/quartictech/circleci-utils/develop/circleci-gcloud-login | bash
      - ./build.sh
